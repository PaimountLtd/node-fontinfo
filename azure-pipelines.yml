trigger:
  branches:
    include:
      - streamlabs
  tags:
    include:
      - '*'

variables:
  BuildDirectory: build
  Generator: Visual Studio 16 2019
  DistributeDirectory: distribute
  FullDistributePath: $(BuildDirectory)\$(DistributeDirectory)
  RuntimeURL: https://atom.io/download/atom-shell
  RuntimeName: iojs
  RuntimeVersion: v9.3.3

jobs:
- job: 'Windows'
  pool: 'Default'
  workspace:
    clean: all

  steps:
  - checkout: self
    fetchDepth: 10

  - powershell: 'Write-Host "##vso[task.setvariable variable=tagartifact]node-fontinfo-$((git.exe describe --tags --abbrev=0) | Out-String)"'
    displayName: 'Get tag for artifact'

  - powershell: git config --global core.autocrlf false
    displayName: 'Set Unix checkout for git'

  - script: 'yarn install'
    displayName: 'Install dependencies'

  - script: 'cmake -H. -B"%BuildDirectory%" -G"%Generator%" -A x64 -DCMAKE_INSTALL_PREFIX="%FullDistributePath%\node-fontinfo" -DNODEJS_NAME=%RuntimeName% -DNODEJS_URL=%RuntimeURL% -DNODEJS_VERSION=%RuntimeVersion%'
    displayName: 'Configure node-fontinfo'

  - script: 'node ci/bump-version.js'
    displayName: 'Bump version number'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))

  - script: 'cmake --build %BuildDirectory% --target install --config RelWithDebInfo'
    displayName: 'Build node-fontinfo'

  - script: 'git clone https://github.com/stream-labs/symsrv-scripts.git'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))
    displayName: 'Fetch symsrv-scripts'

  - script: ./symsrv-scripts/main.bat "%CD%/symsrv-scripts" ".\main.ps1 -localSourceDir '%CD%' -repo_userId 'stream-labs' -repo_name 'node-fontinfo' -repo_branch '$(Build.SourceVersion)'"
    displayName: 'Run symbol server scripts'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))
    env:
      AWS_SYMB_ACCESS_KEY_ID: $(AWS_SYMB_ACCESS_KEY_ID)
      AWS_SYMB_SECRET_ACCESS_KEY: $(AWS_SYMB_SECRET_ACCESS_KEY)

  - task: ArchiveFiles@2
    displayName: 'Generate artifact'
    inputs:
      rootFolderOrFile: $(FullDistributePath)\node-fontinfo
      includeRootFolder: true
      archiveType: tar
      tarCompression: gz
      archiveFile: '$(tagartifact)-win64.tar.gz'

  - task: PublishBuildArtifacts@1
    displayName: 'Publish build artifact'
    inputs: 
      pathtoPublish: $(Build.SourcesDirectory)\$(tagartifact)-win64.tar.gz
      artifactName: $(tagartifact)-win64.tar.gz

  - task: S3Upload@1
    inputs:
      awsCredentials: 'SLOBS_BE_AWS'
      regionName: 'us-west-2'
      bucketName: 'slobs-node-fontinfo'
      globExpressions: '$(tagartifact)-win64.tar.gz'
      filesAcl: 'public-read'
    displayName: 'Upload tag artifact to S3'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))

- job: 'MacOSX'
  pool:
    vmImage: 'macOS-11'
  workspace:
    clean: all

  steps:
  - checkout: self
    fetchDepth: 10

  - powershell: 'Write-Host "##vso[task.setvariable variable=tagartifact]node-fontinfo-$((git describe --tags --abbrev=0) | Out-String)"'
    displayName: 'Get tag for artifact'

  - task: NodeTool@0
    displayName: 'Install Node'
    inputs:
      versionSpec: '14.x'

  - script: 'yarn install'
    displayName: 'Install dependencies'

  - script: 'node ci/bump-version.js'
    displayName: 'Bump version number'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))

  - script: './ci/build-osx.sh'
    displayName: 'Build fontinfo'

  - task: ArchiveFiles@2
    displayName: 'Generate artifact'
    inputs:
      rootFolderOrFile: build/distribute/node-fontinfo
      includeRootFolder: true
      archiveType: tar
      tarCompression: gz
      archiveFile: '$(tagartifact)-osx.tar.gz'

  - task: PublishBuildArtifacts@1
    displayName: 'Publish build artifact'
    inputs: 
      pathtoPublish: $(Build.SourcesDirectory)/$(tagartifact)-osx.tar.gz
      artifactName: $(tagartifact)-osx.tar.gz

  - task: S3Upload@1
    inputs:
      awsCredentials: 'SLOBS_BE_AWS'
      regionName: 'us-west-2'
      bucketName: 'slobs-node-fontinfo'
      globExpressions: '$(tagartifact)-osx.tar.gz'
      filesAcl: 'public-read'
    displayName: 'Upload tag artifact to S3'
    condition: and(succeeded(), contains(variables['Build.SourceBranch'], 'tags'))